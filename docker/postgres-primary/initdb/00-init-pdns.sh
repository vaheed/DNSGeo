#!/usr/bin/env bash
set -euo pipefail

echo "[postgres-init] first-boot application schema is managed by the db-init service."
echo "[postgres-init] this keeps fresh and existing PostgreSQL volumes on the same migration path."
